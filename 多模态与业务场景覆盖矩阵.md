# 多模态与业务场景覆盖矩阵

> 目的：用同一套 Model Factory、Serving 和 Reliable Runtime 支持不同模态、运行环境和业务场景。场景不是独立复制的项目，而是共享底座上的可验证 Environment Adapter、任务集和策略包。

## 1. 四个正交维度

### 1.1 模态

| 模态 | 典型输入 | 典型输出 | 关键指标 |
|---|---|---|---|
| Text | 指令、日志、代码、Schema | 文本、JSON、Tool Call | Accuracy、F1、Validity、Pass@K |
| Image / GUI | 截图、区域图、图表、扫描件 | bbox、ref、动作、解释 | Grounding Accuracy、IoU、Task Success |
| Document | PDF、Office 文档、长表格 | 结构化字段、引用、摘要、编辑建议 | Extraction F1、Citation Accuracy、Layout Robustness |
| Audio | 语音、提示音、会议片段 | ASR、意图、动作、语音响应 | WER、Intent Accuracy、Latency |
| Video | 屏幕录制、操作序列、场景视频 | 事件、状态变化、动作 | Temporal Accuracy、Event F1、Frame/Token Cost |
| Multimodal Fusion | 图文、音画、文档+表格 | 统一状态和动作 | Cross-modal Consistency、Task Success |

### 1.2 运行环境

| 环境 | Observation | Action | 主要风险 |
|---|---|---|---|
| Desktop GUI | UIA、截图、OCR、窗口状态 | click/type/key/scroll/drag | 过期 ref、焦点错误、误操作 |
| Browser | DOM、页面文本、截图、网络状态 | navigate/click/form/download | 登录态、页面变化、最终提交 |
| Code / Shell / Git | 仓库、文件、测试、只读命令 | edit/test/branch/commit | 任意命令、破坏性变更、秘密泄漏 |
| Database / Table | Schema、SQL plan、样例行 | query/export/approved write | 大扫描、越权、错误写入 |
| Enterprise API | 表单、工单、状态、权限 | create/update/approve | 重复写、RBAC、补偿 |
| Observability / K8s | metric/log/trace/resource | diagnose/scale/rollback | 生产变更、错误归因 |
| Simulator | 传感器、世界状态、轨迹 | discrete/continuous action | 实时性、碰撞、分布偏移 |

### 1.3 模型任务

| 任务 | 产物 | 训练方式 |
|---|---|---|
| Perception / Extraction | 文本、实体、版面、事件 | SFT、分类、检测 |
| Grounding | bbox、ref、DOM node、时间段 | SFT、对比学习、可验证奖励 |
| Tool Use / Action | tool + arguments + risk | SFT、蒸馏、DPO/GRPO |
| Planning | 子目标、步骤、停止条件 | SFT、搜索、Agentic RL |
| Retrieval / Reranking | 候选、排序、证据 | 对比学习、Hard Negative |
| Verifier / Reward | success、error、risk、score | 分类、Pairwise、Reward Model |
| Generation | 文本、图像、音频或视频 | SFT、Preference、生成模型专项训练 |

### 1.4 业务场景

| 场景 | 推荐模态与环境 | 最能证明 |
|---|---|---|
| Desktop GUI Agent | Text+Image / Desktop | Grounding、Tool Use、可靠执行 |
| Document Intelligence | Document+Image / Desktop or API | 版面理解、抽取、引用、长上下文 |
| Browser Research | Text+Image / Browser | 搜索、来源追踪、页面变化恢复 |
| Coding Agent | Text+Code / Code+Shell+Git | 检索、编辑、测试、恢复、Eval |
| Enterprise Workflow | Text+Document / API+Desktop | 状态机、审批、幂等、RBAC、审计 |
| Data Agent | Text+Table / Database | SQL、安全执行、结果验证、成本 |
| DevOps Agent | Text+Logs / Observability+K8s | 诊断、Runbook、审批、回滚 |
| Security Agent | Text+Graph+Logs / API+Observability | 证据链、风险分级、最小权限 |
| Audio / Video Agent | Audio+Video+Text / Streaming | 时序理解、流式推理、跨模态融合 |
| Robotics / Driving | Video+State / Simulator | VLA、闭环控制、安全约束 |

### 1.5 执行拓扑

| 拓扑 | 适用条件 | 必须证明 |
|---|---|---|
| Single-Agent | 默认路径；任务可由一个受控执行循环完成 | 正确性、成本、恢复和安全 |
| Multi-Agent | 复杂任务存在明确的专业化、并行探索或独立审查收益 | 委派、共享状态、冲突、恢复、预算和相对收益 |

Multi-Agent 是正式的 Project H 和第四个深度 Lab，但不是业务场景。它必须选择一个现有 `SCN` 场景作为验证载体，首选 `SCN-004 Coding Agent`。

## 2. 场景统一契约

每个场景必须包含以下文件或等价产物：

```text
environments/<scenario>/
├── manifest.yaml
├── observation_schema.json
├── action_schema.json
├── adapter.py
├── verifier.py
├── policy.yaml
├── tasks/
│   ├── train.jsonl
│   ├── validation.jsonl
│   └── test.jsonl
├── fixtures/
├── tests/
└── README.md
```

### 2.1 Manifest 必填字段

```yaml
scenario_id: SCN-XXX
scenario_version: 0.1.0
modalities: []
environment_kind: ""
observation_schema_version: ""
action_schema_version: ""
policy_version: ""
task_set_version: ""
verifier_version: ""
runtime_min_version: ""
reset_strategy: ""
side_effect_class: read_only
```

### 2.2 统一验收

每个场景都必须具备：

1. **Environment Adapter**：只转换 Observation/Action，不复制 Runtime。
2. **Dataset**：训练、验证和冻结测试集独立版本。
3. **State Verifier**：根据环境状态判断，不接受模型自报成功。
4. **Safety Policy**：定义权限、审批、预算和禁止动作。
5. **Eval Report**：质量、安全、可靠性、成本和延迟。
6. **Failure Cases**：至少包含环境变化、工具失败、超时和恢复。
7. **Serving Benchmark**：记录模态编码、推理和端到端延迟。
8. **Demo**：固定脚本、固定版本、可重复运行。

## 3. 场景实施矩阵

| ID | 场景 | 优先级 | 首版范围 | 暂不包含 |
|---|---|---:|---|---|
| SCN-001 | Desktop GUI | P0 | UIA+截图+OCR、只读/低风险动作、状态验证 | 任意应用自动化 |
| SCN-002 | Document / Chart / PDF | P1 | 抽取、引用、表格/图表问答、编辑建议 | 任意格式完美解析 |
| SCN-003 | Browser Research | P1 | 搜索、翻页、提取、引用、下载前确认 | 绕过登录和反爬 |
| SCN-004 | Coding Agent | P1 | Issue→计划→编辑→测试→补丁摘要 | 无审批提交/推送 |
| SCN-005 | Enterprise Workflow / Data | P2 | 表单、规则、只读 SQL、审批、结果回写 | 生产数据库自主写入 |
| SCN-006 | DevOps Agent | P2 | 日志/指标/Runbook、只读诊断、变更建议 | 无审批生产变更 |
| SCN-007 | Security Agent | P2 | 告警关联、风险分级、调查清单、证据建议 | 自主隔离或攻击操作 |
| SCN-008 | Audio / Video Agent | P3 | 短音频/短视频理解、时序事件和提示响应 | 长视频生成、全双工产品化 |
| SCN-009 | Robotics / Driving Simulator | P3 | 离散动作、状态验证、碰撞/越界安全 | 真实车辆或机器人控制 |

Multi-Agent 不单独占用 `SCN` 编号。它由 `MA-001～010` 管理，必须复用上述场景之一，并保留 Single-Agent 路径作为基线和回退。

## 4. 分波次实施

### Wave 1：证明主闭环

- SCN-001 Desktop GUI；
- MVP-1 文本 Tool Router；
- MVP-2 图文 GUI Action Model；
- 固定 Eval、Runtime 接入和 Badcase 回流。

### Wave 2：证明跨环境复用

从以下选择两个：

- SCN-002 Document；
- SCN-003 Browser Research；
- SCN-004 Coding Agent。

验收重点：不修改核心训练、Serving 和 Runtime，只新增 Adapter、任务和 Verifier。

### Wave 3：证明企业安全和系统能力

- SCN-005 Enterprise Workflow / Data；
- SCN-006 DevOps；
- SCN-007 Security。

验收重点：RBAC、审批、幂等、审计、补偿和回滚。

### Wave 4：证明原生多模态和物理环境迁移

- SCN-008 Audio / Video；
- SCN-009 Robotics / Driving Simulator。

验收重点：时序 Observation、流式 Serving、实时预算和安全约束。

### Project H：证明多 Agent 协调系统

- 首选 SCN-004 Coding Agent；
- Coordinator、Researcher、Implementation Worker、Reviewer/Integrator；
- Typed Message、受限委派、共享 Durable State、Lease 和 Worker Recovery；
- 使用同一冻结任务集比较 Single-Agent 与 Multi-Agent；
- 收益不能覆盖成本、延迟和新增故障面时，不替换 Single-Agent。

## 5. 复用规则

- 所有场景复用同一 Dataset Registry、Model Registry 和 Eval Hub。
- 所有动作复用同一 Runtime、Policy、Approval、WAL 和 Trace。
- 场景 Adapter 不保存第二份任务真相。
- 场景 Verifier 可以读环境状态，但不能执行动作。
- 新场景不得要求模型直接访问 Driver、Shell、数据库或生产 API。
- 跨场景训练必须报告每个场景的独立指标，不能只给平均分。
- 新场景导致旧场景退化时，必须阻止发布或启用显式路由。

## 6. 场景覆盖看板

状态定义：

- `L0 Named`：仅进入路线；
- `L1 Contract`：Schema、Policy、任务和 Verifier 已设计；
- `L2 Offline`：离线数据、模型和评测已通过；
- `L3 Environment`：真实/仿真环境验证通过；
- `L4 Closed Loop`：Badcase 已回流并证明新版本提升。

| 场景 | 当前目标 | 进入下一等级的证据 |
|---|---|---|
| SCN-001 Desktop GUI | L1 → L3 | 固定 GUI 任务、动作后状态验证、故障恢复 |
| SCN-002 Document | L0 → L1 | 文档 Schema、引用/表格 Eval、Verifier |
| SCN-003 Browser Research | L0 → L1 | DOM+视觉契约、来源和页面变化任务 |
| SCN-004 Coding Agent | L0 → L1 | Repo fixture、补丁/测试 Verifier、安全 Shell |
| SCN-005 Workflow/Data | L0 → L1 | 表单/SQL Schema、RBAC、审批和幂等任务 |
| SCN-006 DevOps | L0 → L1 | Log/Metric fixture、Runbook、回滚策略 |
| SCN-007 Security | L0 → L1 | 告警/资产 Schema、风险和证据评测 |
| SCN-008 Audio/Video | L0 → L1 | 时序 Observation、流式预算和事件 Eval |
| SCN-009 Simulator | L0 → L1 | Simulator Adapter、状态/动作和安全 Verifier |

## 7. 选择规则

- 首个场景固定为 Desktop GUI，因为它已经拥有真实 Runtime 和证据。
- 第二环境优先选择 Document、Browser 或 Coding，不同时做三个。
- 求职方向决定第三场景：企业岗位选 Workflow/Data，AI Infra 选 DevOps，安全岗位选 Security。
- Audio/Video 和 Simulator 用于扩展原生多模态，不阻塞首版。
- 场景数量不是指标；跨场景复用、独立评测和失败归因才是指标。
