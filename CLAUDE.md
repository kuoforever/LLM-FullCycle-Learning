# Claude Code guide

Follow [AGENTS.md](AGENTS.md). Start with
[PROJECT_STATUS.md](PROJECT_STATUS.md).

If the active task is `GDA-FC-*`, switch to:

```text
C:\Users\Alienware\guarded-desktop-agent
```

Then follow that repository's `CLAUDE.md` and `PROJECT_STATUS.md`. Do not edit
both repositories speculatively in one change; update the Full Cycle consumer
only after the Runtime contract is implemented and tested.

At completion, report:

1. outcome;
2. modified files;
3. tests and exact results;
4. limitations;
5. next task.
