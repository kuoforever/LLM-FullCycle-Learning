# 观察记录表：Dify / LangGraph / 自研 runtime 三方对照

> **这张表才是这两天真正的产物。** 工作流本身做完就放着，但这张表里的每一行都是面试时能直接说出口的具体材料。
>
> **填写纪律**：
> - 只填**亲手验证过**的。没测到的写「**未测**」，不要凭直觉猜，也不要留空。
> - 面试时被问到写着「未测」的那一条，就直接说没测到——比说错强得多。
> - 「自研 runtime」那一栏是已知的，先填好，做对照时才有参照系。

---

## 一、失败处理

### 1.1 节点 / 工具调用失败后的默认行为

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | 动作后失效链路强制重新观察；ref 动作在元素失效或被遮挡时**显式失败**，不退化成中心点坐标点击；未注册的工具、副作用、结果字段与停止码一律**失败关闭** |

**能不能配置成"退回上一步重新取证"？**

| | |
|---|---|
| Dify | |
| LangGraph | |

**失败信息会传给下游节点吗？下游能看到失败原因吗？**

| | |
|---|---|
| Dify | |
| LangGraph | |

---

### 1.2 「已派发但结果未知」这个状态怎么表达 ⭐

> **这是整张表最重要的一行。** 我系统的核心设计就是把它单独识别出来并拒绝自动重放。两个框架有没有这个概念，直接决定了对比的深度。

**问题**：节点执行到一半进程崩了，恢复时框架认为这个节点跑过没有？会不会重跑？

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | 预写日志 + 比较交换 + 原子检查点，把每一步区分为**未执行 / 已完成 / 结果未知**三态；"已派发但没有完成记录"判为结果未知，**停下来交人工，绝不自动重放**。崩溃注入基准 30 次运行 × 每次 100 条任务，每个具名故障点强制杀进程后于新进程恢复，**0 次重复副作用** |

**如果框架会重跑，它提供了什么手段让我自己避免重复副作用？**（幂等键？去重表？还是完全靠使用者自己做？）

| | |
|---|---|
| Dify | |
| LangGraph | |

---

## 二、状态与持久化

### 2.1 状态存在哪、什么粒度

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph**（checkpointer） | |
| **自研 runtime** | 追加式 campaign ledger（每条策略决策、观察、调用-结果配对都落盘）+ 原子检查点；长任务另有 lease 与 heartbeat |

**存的是完整快照还是增量？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | 追加式账本（增量）+ 检查点（快照）；**增量 / 差分观察快照仍为已设计未实现** |

---

### 2.2 跨进程恢复 ⭐

> **这条要亲手做一次演示**：起一个全新进程、只给标识符，看能不能接着上一步继续。这是 Day 2 最有说服力的产物。

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | deterministic handoff：新进程**不依赖旧模型对话**即可续跑；任务耗尽时自动写出交接并退役对应心跳，重复执行幂等 |

**实际演示做了吗？** ☐ 做了（截图 / 录屏位置：____________）　☐ 没做

---

### 2.3 状态更新的合并规则

**两条边 / 两个分支同时更新同一个字段会怎样？**

| | |
|---|---|
| Dify | |
| LangGraph（reducer） | |
| 自研 | 单一 Runner 串行派发边界，不存在并发写同一状态的情况——**这是拿吞吐换确定性的取舍** |

---

## 三、上下文管理

### 3.1 上下文超长时的行为 ⭐

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | 按事件预算归约账本视图，强制保留原始用户任务、最新模型轮次与后续延续、每条策略决策、最近一次已验证观察、完整的身份关联调用-结果配对，并保留**显式截断标记**；**必需事件超预算时在发出下一次请求之前失败退出，不静默丢弃** |

**能不能强制指定"这几段必须保留"？**

| | |
|---|---|
| Dify | |
| LangGraph | |

**是静默截断、自动摘要、还是报错？**（**静默截断是最危险的一种**，模型不会告诉你它少了证据）

| | |
|---|---|
| Dify | |
| LangGraph | |

---

## 四、人机介入

### 4.1 有没有原生的暂停 / 审批机制

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph**（`interrupt`） | |
| **自研 runtime** | 四选项决策卡：**批准 / 拒绝 / 同轮重新观察 / 持久化延后**；桌面原生 UI，主动取焦但不置顶、可拖动缩放、证据摘要可滚动、超时行为确定 |

---

### 4.2 暂停之后的状态是持久的吗 ⭐

**进程退出再起来，还停在原处吗？还是只是内存里的一个 await？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | **延后是持久化且明确不可自动恢复的状态**——不是"稍后自动重试" |

---

### 4.3 语义区分

**"拒绝"和"延后"在框架里是同一个状态还是两个？"重来一次"必须基于新证据吗？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | 四种决策语义各不相同；**重新观察必须基于新观察，旧轮次直接作废**；四种决策仍只能经单一 Runner 边界派发 |

**超时了默认走哪条路？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | 超时行为确定（不是未定义行为） |

---

## 五、审计与可观测

### 5.1 能不能回答"这一步动作是谁、基于什么授权的"

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | 三层分工：**追加式审计日志**回答"谁授权跨了边界"、**持久化检查点**回答"什么可恢复"、**遥测**只回答"聚合快慢与失败率"；**遥测永不作为动作已发生的证据** |

### 5.2 运行日志的粒度到哪一层？能不能事后完整回放一次运行？

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | 从账本可完整重放，**不依赖模型自述** |

### 5.3 可观测接入是必需依赖还是可选依赖？离线 / 无网能不能跑？

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | OpenTelemetry 与工作流引擎均为**可选依赖隔离**，主包不导入，离线运行与 CI 不依赖任何外部采集器 |

---

## 六、评测与回归

### 6.1 有没有批量运行 / 测试集 / 版本对比

| | 观察结果 |
|---|---|
| **Dify** | |
| **LangGraph** | |
| **自研 runtime** | 六级评测契约 + 13 个确定性 case，判定依据是**调用序列与安全结果的 trace 比对**而非自然语言答案 |

### 6.2 防漂移 ⭐

**改了 prompt 之后，有没有任何机制阻止评测集被顺手改软？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | case JSON 由 **SHA-256 manifest 冻结进 CI**——改 prompt 时改不动评测集 |

### 6.3 模型返回的结构化输出，框架会替我校验吗

**还是原样塞进 state / 变量？会不会把"模型声称做过"当成"真的做过"？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | 模型计划作为**不可信声明式数据重新校验**；返回必须命中精确有界的结果结构、**只能声明真正执行过的观察工具** |

---

## 七、扩展性

**加一个新工具 / 新节点，要改多少地方？会不会需要动核心？**

| | |
|---|---|
| Dify | |
| LangGraph | |
| 自研 | manifest 路由的场景契约：15 项已评审能力组合成场景，**注册新场景不改执行边界与长任务控制流**；扩展性不靠放宽约束——未注册的场景、工具、副作用、结果字段与停止码一律失败关闭 |

---

## 八、三方对比小结（200 字，写完要能脱稿讲）

> 提示：不要写成"Dify 简单 LangGraph灵活自研可控"这种谁都会说的废话。
> **挑一到两条你实测出来的、有具体差异的点**，说清楚：哪个框架、默认行为是什么、我自己那套怎么做的、为什么这么选、代价是什么。

```
（在此写 200 字）




```

---

## 九、遗留问题

> 没测到的、存疑的、想再深入的，都记在这里。面试被问到时直接说"这条我没测到"——比编强得多。

- 
- 
- 
